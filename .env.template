APP_BASE_URL=http://127.0.0.1:8000
APP_HOST=0.0.0.0
APP_PORT=8000
LOG_LEVEL=INFO
DEPLOYMENT_TARGET=local
STORAGE_BACKEND=sqlite
EXPORT_BACKEND=local

STRAVA_CLIENT_ID=
STRAVA_CLIENT_SECRET=
STRAVA_WEBHOOK_VERIFY_TOKEN=
STRAVA_WEBHOOK_CALLBACK_URL=
STRAVA_REDIRECT_URI=http://127.0.0.1:8000/auth/strava/callback
STRAVA_SCOPES=read,activity:read_all,profile:read_all
CRON_SECRET=

DATABASE_PATH=data/db/strava_activity_sync.sqlite3
EXPORT_DIR=data/exports
TIMEZONE=Europe/Madrid
SYNC_LOOKBACK_DAYS=30
RECONCILIATION_INTERVAL_MINUTES=16
RECONCILE_LOOKBACK_DAYS=14
SYNC_BATCH_SIZE=32
STRAVA_REQUEST_TIMEOUT_SECONDS=30
STRAVA_VERIFY_SSL=true
STRAVA_CA_BUNDLE_PATH=
VERCEL_BLOB_STATE_PATH=strava/state/strava-state.json
VERCEL_BLOB_EXPORT_PREFIX=strava/exports
VERCEL_BLOB_ACCESS=private

ENABLE_DRIVE_EXPORT=false
GOOGLE_DRIVE_FOLDER_ID=
GOOGLE_DRIVE_SERVICE_ACCOUNT_JSON=

# Reserved for the planned Supabase backend migration.
# The current Python service does not read these values yet.
APEX_SUPABASE_URL=
APEX_SUPABASE_SERVICE_ROLE_KEY=
APEX_SUPABASE_SCHEMA=public

# Frontend-style variables are documented here only to keep the naming contract
# aligned with the wider APEX ecosystem. This backend service does not consume
# them directly.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_SUPABASE_USER_ID=sergio

# Optional direct Postgres connection settings for future server-side work that
# needs a database connection string instead of the Supabase REST interface.
SUPABASE_DB_HOST=
SUPABASE_DB_PORT=5432
SUPABASE_DB_NAME=postgres
SUPABASE_DB_USER=postgres
SUPABASE_DB_PASSWORD=
SUPABASE_DB_SSLMODE=require
SUPABASE_STORAGE_BUCKET=strava-context
